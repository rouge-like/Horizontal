# 20251004 일일업무일지

## 작업 요약
- `AUsableItemBase`를 재구성하여 아이템 이름, 장착 타입, 픽업/드롭/장착/사용 훅을 정의하고 기본 구현을 정리함.
- `UInventoryComponent`를 현재 장착 아이템을 `SelectedItem`으로 관리하는 구조로 변경하고, 인벤토리를 `TWeakObjectPtr` 배열로 유지함.
- `SelectedItem`을 복제 대상으로 설정하고 `ServerSelectItem` RPC와 `OnRep_SelectedItem`을 구현하여 서버가 선택 상태를 확정하고 모든 클라이언트에 동기화되도록 함.

## 향후 계획
1. 아이템 추가/제거 요청을 서버 권한 흐름으로 제한할지 결정하고 필요 시 RPC를 추가.
2. 인벤토리 전체 슬롯 정보의 복제 및 HUD 노출 방식 확정.
