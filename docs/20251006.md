# 20251006 업무일지

## 작업 요약
- 인벤토리에서 서버 권한일 때 첫 슬롯에 자동 장착되도록 `AddItem` 로직을 조정해 초기 장착을 보장했습니다.
- `AUsableItemBase`에 임시 소켓 문자열 `tmpSocket`과 장착 상태 플래그를 추가하고, 1인칭 메시 소켓에 바로 붙도록 부착/가시성 흐름을 정리했습니다.
- `OnPickup` 순서를 재구성해 `OnEquip` 실행 시점에 소유자 정보가 준비되도록 하고, 복제 지연을 `OnRep_Owner` 재시도로 보완했습니다.

## 다음 계획
1. 아이템 드롭 시 월드 재배치, 충돌/가시성 복구, 필요 시 물리 반영까지 포함한 `OnDrop` 로직을 구현합니다.
